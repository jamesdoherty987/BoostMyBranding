{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm run vercel:build-web"
}
